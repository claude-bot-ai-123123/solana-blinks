# Solana Blinks Configuration

# Required: Solana RPC endpoint
# Use a premium RPC for production (Helius, QuickNode, Triton, etc.)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Required for write operations: Wallet configuration
# Option 1 (Recommended): Path to keypair JSON file
SOLANA_WALLET_PATH=~/.config/solana/id.json

# Option 2 (Fallback): Direct private key - LESS SECURE, avoid in production
# Format: Base58 string OR JSON array [1,2,3,...]
# SOLANA_PRIVATE_KEY=your-private-key-here

# Optional: Dialect API key for higher rate limits
DIALECT_API_KEY=

# Optional: Override default Dialect API URL
# DIALECT_API_URL=https://api.dialect.to
